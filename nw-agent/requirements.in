# NeuralWarden VM Agent — direct dependencies (pip-tools input)
#
# Edit THIS file to add/change dependencies, then regenerate:
#   pip-compile --generate-hashes requirements.in -o requirements.txt
#
# Security policy: all packages must have pinned versions and SHA-256 hashes
# in requirements.txt (generated by the command above).

# HTTP client — CVE-2023-32681 / PYSEC-2023-74 patched in >=2.31.0
requests>=2.31.0
