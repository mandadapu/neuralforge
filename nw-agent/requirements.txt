# Security: requests>=2.31.0 fixes CVE-2023-32681 / GHSA-9wx4-h78v-vm56
# (Proxy-Authorization header leak on redirect in requests<=2.28)
#
# Compliance notes for consumers of this library:
#   - All outbound HTTP calls must use HTTPS (TLS). Do not disable verify=True.
#   - Set explicit timeouts on every request (e.g. timeout=(5, 30)) to bound
#     data-exposure windows and prevent indefinite hangs.
#   - Do not log response bodies that may contain sensitive data (PII, tokens).
#   - Audit log destination URL and data classification for each call site.
#   - Certificate pinning required for endpoints handling PHI, PCI, or financial data.
requests>=2.31.0
