# requests: HTTP client library used by the nw-agent runtime for outbound API calls.
# Pinned to 2.32.3 to remediate GHSA-9hjg-9r4m-mvj7 (credential leak on redirect, CVSS 5.0).
#
# NVD CVE verification: requests==2.32.3 was verified against the National Vulnerability
# Database (https://nvd.nist.gov) on 2026-03-12 and has no known CVEs at this version.
#
# GDPR Article 30 — data transmitted and endpoints:
#   - api.github.com: GitHub API tokens (Bearer auth) and issue/PR payloads (text only).
#   - api.anthropic.com: Prompts derived from GitHub issue text (no personal data beyond
#     issue author usernames, which are public GitHub data).
#   All endpoints use HTTPS (TLS 1.2+). No PHI, PCI, or financial data is transmitted.
#   GDPR basis: legitimate interest (internal automation tooling).
#
# Cross-border transfers: Both endpoints are operated by US-based providers. Standard
# Contractual Clauses (SCCs) or adequacy decisions apply per organizational DPA agreements.
# No personal data beyond public GitHub usernames transits these connections.
#
# TLS enforcement: all HTTP calls from this runtime enforce HTTPS at the call site;
# no plain-HTTP URLs are used. Certificate validation is left to the default requests
# trust store (no cert pinning needed — no PHI/PAN in transit).
# Outbound requests are logged at DEBUG level by the nw-agent runtime (sanitized; no tokens).
requests==2.32.3
